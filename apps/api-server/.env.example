# api-server が実際に読む環境変数。
# AUTH0_SECRET は beatfolio と同じ値にする（同一のセッション Cookie を復号するため）。

DATABASE_URL=''

AUTH0_SECRET=''

ENABLE_BASIC_AUTH=false
BASIC_AUTH_USERNAME=
BASIC_AUTH_PASSWORD=

# ローカル開発サーバーのポート（未設定なら 3001）
PORT=3001
