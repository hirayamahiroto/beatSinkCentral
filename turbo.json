{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": [".next/**", "!.next/cache/**"]
    },
    "beatfolio#build": {
      "outputs": [".next/**", "!.next/cache/**", "dist/**"],
      "env": [
        "AUTH0_SECRET",
        "AUTH0_DOMAIN",
        "AUTH0_CLIENT_ID",
        "AUTH0_CLIENT_SECRET",
        "APP_BASE_URL",
        "API_SERVER_BASE_URL",
        "ENABLE_BASIC_AUTH",
        "BASIC_AUTH_USERNAME",
        "BASIC_AUTH_PASSWORD"
      ]
    },
    "api-server#build": {
      "outputs": [".next/**", "!.next/cache/**", "dist/**"],
      "env": ["DATABASE_URL", "API_SERVER_BASE_URL"]
    },
    "lint": {
      "dependsOn": ["^lint"],
      "outputs": []
    },
    "check-types": {
      "dependsOn": ["^check-types"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "start": {
      "dependsOn": ["build"]
    },
    "test": {}
  },
  "globalDependencies": ["**/.env.*local"]
}
